Section "Screen"
# Prefer a 24-bit default depth so X and xrdp negotiate a 24bpp visual.
    Identifier "Screen0"
    DefaultDepth 24
    SubSection "Display"
        Depth 24
    EndSubSection
EndSection
